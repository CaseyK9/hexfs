security:
  # The key to upload.
  masterkey:
  # Set the extension blacklist. If an extension is also in the whitelist, it will still be blacklisted.
  # If this is not set, there will be no blacklist.
  blacklist:
    - exe
    - sh
    - doc
    - docx
    - html
    - mhtml
  # Set the extension whitelist.
  # If this is not set, there will be no whitelist.
  whitelist:
    - png
    - jpeg
    - gif
    - mp3
    - mp4
    - jpg
    - apng
    - mov
  # Max size (in bytes) a file can be. This is 50 MiB
  maxsizebytes: 52428800
  # Key not required to upload if this is true.
  publicmode: false

server:
  # What port to listen on. Default is 3030
  port:
  # ID length to use. (e.g: xxxxx.png has an ID length of 5)
  idlen: 5

net:
  redis:
    uri:
    password:
    db: 0
  gcs:
    # AES-256 Base64 encoded key
    secretkey:
    bucketname:
